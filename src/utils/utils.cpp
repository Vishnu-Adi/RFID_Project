#include "utils.h"

double calculateFitness(const Solution& solution) {
    // Implement fitness calculation for GA
    return 0.0;
}

double calculateFitness(const std::vector<double>& position) {
    // Implement fitness calculation for PSO
    return 0.0;
}