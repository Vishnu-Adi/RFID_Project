#ifndef CONFIG_H
#define CONFIG_H

#include "../utils/utils.h"

const Parameters DEFAULT_PARAMETERS = {
    50,   // populationSize
    100,  // maxGenerations
    30,   // swarmSize
    1000  // maxIterations
    // Initialize other parameters as needed
};

#endif // CONFIG_H